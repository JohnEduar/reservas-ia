# Variables requeridas por el servicio db en docker-compose.yml
# Copia este archivo a .env y completa los valores antes de correr docker compose up

DB_PASSWORD=your_secure_mysql_password
DB_NAME=glampbook
